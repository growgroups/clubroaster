(function(){
'use strict';
function coordinatorRows(){const all=[...futureRoster].sort((a,b)=>a.iso.localeCompare(b.iso)||a.time.localeCompare(b.time));if(seasonRosterState.view==='upcoming')return all.slice(0,6);if(seasonRosterState.view==='nextweek')return all.filter(r=>r.iso<='2026-08-29');if(seasonRosterState.view==='month')return all.filter(r=>r.iso<'2026-10-01');return all}
coordinatorSeasonPlanner=function(){const rows=coordinatorRows(),planned=rows.length,offered=rows.filter(r=>r.status==='Offered').length,peopleWithAvailability=new Set(futureAvailability.map(a=>a.person)).size,swaps=swapRequests.filter(s=>!['Approved','Declined','Rejected by coordinator'].includes(s.status)).length;return `<div class="appHero"><small>Forward planning</small><strong>Season roster planner</strong><small>${seasonRosterState.view==='upcoming'?'Next appointments':seasonRosterState.view==='nextweek'?'Through next week':seasonRosterState.view==='month'?'Through September':'Full season'} · schedule using advance availability.</small></div><div class="quickGrid"><div class="appCard"><h3>${planned}</h3><div class="appMeta">Assignments in this view</div></div><div class="appCard"><h3>${offered}</h3><div class="appMeta">Awaiting response</div></div><div class="appCard"><h3>${peopleWithAvailability}</h3><div class="appMeta">Availability lodged</div></div><div class="appCard"><h3>${swaps}</h3><div class="appMeta">Open swaps</div></div></div>${rosterViewTabs()}<div class="appCard"><h3>Coverage</h3>${table(['Round','Date','Time','Court','Assigned umpire','Grade','Status'],rows.map(r=>[r.round,r.date,r.time,r.court,r.person,r.grade,statusTag(r.status)]))}</div><div class="appCard"><h3>Availability planning</h3>${table(['Person','From','To','Status','Window','Note'],futureAvailability.map(a=>[a.person,a.from,a.to,statusTag(a.status),a.window,a.note||'-']))}</div><div class="appCard"><h3>Swap approvals</h3>${swapRequests.length?table(['Request','From','To','Status','Action'],swapRequests.map(s=>[s.id,s.from,s.to,statusTag(s.status),s.status==='Awaiting coordinator approval'?`<button class="btn primary" data-season-action="approveSwap" data-id="${s.id}">Approve</button> <button class="btn" data-season-action="rejectSwap" data-id="${s.id}">Reject</button>`:''])):'No swaps'}</div>`};
document.addEventListener('click',e=>{const el=e.target.closest('[data-season-action="view"]');if(!el)return;seasonRosterState.view=el.dataset.view;if(document.getElementById('drawer')?.classList.contains('open')&&document.getElementById('drawerTitle')?.textContent==='Season roster planner'){e.preventDefault();e.stopImmediatePropagation();openDrawer('Season roster planner',coordinatorSeasonPlanner());return}if(page==='mobile')render()},true);
})();
